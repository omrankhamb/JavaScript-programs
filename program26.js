//string is character array

let a = "jay ganesh.."
console.log(a,a.length)


for(let i  = 0 ; i < a.length ; i++)
{
    console.log(a[i])
}

// for of loop is work in javascirpt
for(char of a)
{
    console.log(char)
}

// for each loop does not work om string

// a.forEach(element => {
//     console.log(element)
// }); 

a.array.forEach(element => {
    console.log(elemet)
});
