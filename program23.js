const arr = 'omprasad'

arr = 'soham' // give errr

// when we use const then the value of variable can not be changed and can not intialise again
// but when we talk about array then we can push pop shoft unshift
